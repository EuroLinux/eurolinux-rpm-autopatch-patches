diff --git a/postgresql-12.spec b/postgresql-12.spec
index 1c192a8..a6272ba 100644
--- a/postgresql-12.spec
+++ b/postgresql-12.spec
@@ -1264,6 +1264,15 @@ if [ "$1" -eq 0 ]
        /sbin/ldconfig
 fi

+%posttrans
+if [ -e /usr/eurodb-12 ]; then
+  pgdata_path="$(cat /usr/lib/systemd/system/eurodb12.service | grep =PGDATA= | cut -d'=' -f 3)"
+  if [ ! "$(ls -A /var/lib/pgsql/12/data/)" ]; then
+    rm -rf /var/lib/pgsql/12/data
+    ln -s "$pgdata_path" /var/lib/pgsql/12/
+  fi
+fi
+
 # FILES section.

 %files -f pg_main.lst
