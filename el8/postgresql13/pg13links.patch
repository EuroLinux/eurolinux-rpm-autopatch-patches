diff --git a/postgresql-13.spec b/postgresql-13.spec
index ea3961f..46c57b9 100644
--- a/postgresql-13.spec
+++ b/postgresql-13.spec
@@ -1023,6 +1023,15 @@ if [ "$1" -eq 0 ]
        /sbin/ldconfig
 fi

+%posttrans
+if [ -e /usr/eurodb-13 ]; then
+  pgdata_path="$(cat /usr/lib/systemd/system/eurodb13.service | grep =PGDATA= | cut -d'=' -f 3)"
+  if [ ! "$(ls -A /var/lib/pgsql/13/data/)" ]; then
+    rm -rf /var/lib/pgsql/13/data
+    ln -s "$pgdata_path" /var/lib/pgsql/13/
+  fi
+fi
+
 # FILES section.

 %files -f pg_main.lst
